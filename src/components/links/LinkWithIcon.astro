---
import Link from "./Link.astro"
import type { LinkProps } from "@/types"

type Props = {
  size?: "sm" | "md" | "lg"
  class?: string
} & LinkProps

const { href, size = "md", class: className } = Astro.props

const sizes = {
  sm: "text-sm h-5",
  md: "text-base h-6",
  lg: "text-lg h-7",
}
---

<Link
  href={href}
  class:list={["flex w-fit justify-items-center gap-2", sizes[size], className]}
>
  <slot />
</Link>
