---
type Props = {
  variant?: "primary" | "secondary"
  size?: "sm" | "md" | "lg"
  class?: string
}

const { variant = "primary", size = "md", class: className } = Astro.props

const variants = {
  primary: "text-blue-100 bg-blue-900",
  secondary: "text-teal-200 bg-teal-950",
}

const sizes = {
  sm: "text-xs",
  md: "text-sm",
  lg: "text-base",
}
---

<article
  class:list={[
    "rounded-full px-3 py-1 font-medium",
    sizes[size],
    variants[variant],
    className,
  ]}
>
  <slot />
</article>
